using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveButton : MonoBehaviour {
/*
    Selector selector;
    BoardEnvironmentController boardCon;
    int x, y;

    void Start()
    {
        selector = GameObject.FindGameObjectWithTag("Core").GetComponent<Selector>();
        boardCon = transform.parent.transform.parent.transform.parent.GetComponent<BoardEnvironmentController>();
        x = transform.parent.transform.parent.GetComponent<TileBehaviour>().x;
        y = transform.parent.transform.parent.GetComponent<TileBehaviour>().y;        
    }

    void OnMouseDown()
    {
        SetMoveListenerState();
    }

    void SetMoveListenerState()
    {
        selector.MoveListen(boardCon.GetUnit(x, y));
    }*/
}
